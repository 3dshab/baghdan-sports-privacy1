# نظام القنوات المباشرة 📺

تم تطوير نظام متكامل للقنوات المباشرة مع مشغل فيديو مدمج.

## المميزات ✨

1. **جلب البيانات من Firebase** - القنوات تُحمل مباشرة من قاعدة البيانات
2. **مشغل فيديو متقدم** - باستخدام Chewie و Video Player
3. **واجهة مستخدم جميلة** - تصميم عصري ومتجاوب
4. **دعم البث المباشر** - تشغيل روابط الفيديو المباشرة
5. **إدارة مرنة** - إضافة وتعديل القنوات بسهولة

## البنية 🏗️

### الملفات الجديدة:

```
lib/
├── models/
│   └── live_channel_model.dart          # نموذج القناة المباشرة
├── screens/
│   ├── live_channels_screen.dart        # شاشة قائمة القنوات (محدثة)
│   └── channel_player_screen.dart       # شاشة مشغل الفيديو
├── services/
│   └── firebase_service.dart            # خدمات Firebase (محدثة)
└── utils/
    └── seed_live_channels.dart          # إضافة بيانات تجريبية
```

## كيفية الاستخدام 📖

### 1. إضافة القنوات الافتراضية

قم بتشغيل هذا الكود مرة واحدة لإضافة القنوات التجريبية:

```dart
import 'package:baghdan_sports/utils/seed_live_channels.dart';

// في أي مكان في التطبيق (مثلاً في main.dart)
await seedLiveChannels();
```

### 2. إضافة قناة جديدة يدوياً

```dart
import 'package:baghdan_sports/models/live_channel_model.dart';
import 'package:baghdan_sports/services/firebase_service.dart';

final firebaseService = FirebaseService();

final newChannel = LiveChannelModel(
  name: 'قناة جديدة',
  description: 'وصف القناة',
  url: 'https://example.com/video.m3u8',
  iconName: 'live_tv',
  colorHex: '1BA098',
  order: 5,
  isActive: true,
);

await firebaseService.addLiveChannel(newChannel);
```

### 3. بنية البيانات في Firebase

المجموعة: `live_channels`

```json
{
  "name": "قناة البطولة الرسمية",
  "description": "البث المباشر لجميع مباريات البطولة",
  "url": "https://example.com/stream.m3u8",
  "iconName": "live_tv",
  "colorHex": "7D1E7D",
  "order": 1,
  "isActive": true
}
```

## الحقول المتاحة 📋

### iconName (الأيقونات المدعومة):
- `live_tv` - تلفزيون مباشر
- `sports_soccer` - كرة قدم
- `analytics` - تحليلات
- `mic` - ميكروفون
- `video_library` - مكتبة فيديو
- `play_circle` - دائرة تشغيل

### colorHex (أمثلة على الألوان):
- `1BA098` - أخضر فيروزي
- `7D1E7D` - بنفسجي
- `8B7E3A` - ذهبي
- `1E5A7D` - أزرق
- `FF5722` - برتقالي
- `4CAF50` - أخضر

## أنواع روابط الفيديو المدعومة 🎥

1. **HLS Streams** - `.m3u8`
2. **MP4 Files** - `.mp4`
3. **YouTube Live** - روابط البث المباشر
4. **RTMP Streams** - بروتوكول RTMP

### أمثلة على الروابط:

```dart
// HLS Stream
url: 'https://example.com/stream/playlist.m3u8'

// MP4 File
url: 'https://example.com/videos/match.mp4'

// YouTube Live (يحتاج معالجة خاصة)
url: 'https://www.youtube.com/watch?v=VIDEO_ID'
```

## مميزات مشغل الفيديو 🎬

- ✅ تشغيل تلقائي
- ✅ وضع ملء الشاشة
- ✅ التحكم في الصوت
- ✅ شريط التقدم
- ✅ معالجة الأخطاء
- ✅ إعادة التحميل
- ✅ واجهة عربية

## إدارة القنوات من Firebase Console 🔧

1. افتح Firebase Console
2. اذهب إلى Firestore Database
3. ابحث عن مجموعة `live_channels`
4. يمكنك:
   - إضافة قناة جديدة
   - تعديل قناة موجودة
   - حذف قناة
   - تفعيل/تعطيل قناة (`isActive`)
   - تغيير الترتيب (`order`)

## ملاحظات مهمة ⚠️

1. **الروابط**: تأكد من أن روابط الفيديو صحيحة وتعمل
2. **الأمان**: لا تضع روابط حساسة في الكود، استخدم Firebase
3. **الأداء**: القنوات تُحمل في الوقت الفعلي من Firebase
4. **الترتيب**: استخدم حقل `order` لترتيب القنوات
5. **التفعيل**: استخدم `isActive` لإخفاء/إظهار القنوات

## استكشاف الأخطاء 🔍

### المشكلة: القنوات لا تظهر
**الحل**: تأكد من:
- وجود بيانات في مجموعة `live_channels`
- حقل `isActive` = `true`
- اتصال Firebase صحيح

### المشكلة: الفيديو لا يعمل
**الحل**: تأكد من:
- صحة رابط الفيديو
- دعم التنسيق (HLS, MP4)
- اتصال الإنترنت

### المشكلة: خطأ في التشغيل
**الحل**: 
- اضغط على زر "إعادة التحميل"
- تحقق من صلاحيات الوصول للرابط
- جرب رابط فيديو آخر

## التطوير المستقبلي 🚀

- [ ] دعم الترجمة
- [ ] قائمة التشغيل
- [ ] التسجيل والحفظ
- [ ] الإشعارات عند بدء البث
- [ ] الدردشة المباشرة
- [ ] الإحصائيات والتحليلات

---

تم التطوير بواسطة فريق بطولة كأس بعدان 18 ⚽
