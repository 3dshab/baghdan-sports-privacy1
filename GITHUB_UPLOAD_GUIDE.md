# دليل رفع سياسة الخصوصية على GitHub

## 📋 المتطلبات
- حساب GitHub
- Git مثبت على جهازك
- مستودع (Repository) على GitHub

---

## 🚀 الطريقة الأولى: عبر Terminal (سطر الأوامر)

### الخطوة 1️⃣: التأكد من وجود Git
```bash
git --version
```

إذا لم يكن مثبتاً، قم بتثبيته من: https://git-scm.com/

---

### الخطوة 2️⃣: التحقق من حالة المشروع
```bash
cd /Users/ebrahimshahbain/Desktop/baghdan_sports
git status
```

---

### الخطوة 3️⃣: إضافة ملف سياسة الخصوصية
```bash
# إضافة ملف سياسة الخصوصية فقط
git add PRIVACY_POLICY.md

# أو إضافة جميع الملفات المعدلة
git add .
```

---

### الخطوة 4️⃣: عمل Commit
```bash
git commit -m "Add privacy policy / إضافة سياسة الخصوصية"
```

---

### الخطوة 5️⃣: رفع الملف إلى GitHub
```bash
# إذا كان المستودع موجود بالفعل
git push origin main

# أو إذا كان الفرع الرئيسي اسمه master
git push origin master
```

---

## 🌐 الطريقة الثانية: عبر موقع GitHub مباشرة

### الخطوة 1️⃣: افتح المستودع
1. اذهب إلى: https://github.com
2. سجل الدخول
3. افتح مستودع المشروع (baghdan_sports)

---

### الخطوة 2️⃣: رفع الملف
1. اضغط على زر **"Add file"** في الصفحة الرئيسية للمستودع
2. اختر **"Upload files"**
3. اسحب ملف `PRIVACY_POLICY.md` أو اضغط **"choose your files"**
4. أضف رسالة commit: "Add privacy policy"
5. اضغط **"Commit changes"**

---

### الخطوة 3️⃣: إنشاء رابط مباشر
بعد الرفع، سيكون رابط سياسة الخصوصية:
```
https://github.com/[username]/baghdan_sports/blob/main/PRIVACY_POLICY.md
```

استبدل `[username]` باسم المستخدم الخاص بك على GitHub.

---

## 📱 الطريقة الثالثة: إنشاء صفحة ويب لسياسة الخصوصية

### استخدام GitHub Pages:

#### 1. إنشاء مجلد docs
```bash
mkdir docs
cp PRIVACY_POLICY.md docs/privacy-policy.md
```

#### 2. تفعيل GitHub Pages
1. اذهب إلى إعدادات المستودع (Settings)
2. اختر **Pages** من القائمة الجانبية
3. في **Source**، اختر **main** branch و **/docs** folder
4. اضغط **Save**

#### 3. الوصول إلى الصفحة
بعد دقائق، ستكون سياسة الخصوصية متاحة على:
```
https://[username].github.io/baghdan_sports/privacy-policy
```

---

## 🔗 استخدام الرابط في التطبيق

### في ملف Info.plist (iOS):
```xml
<key>NSPrivacyPolicyURL</key>
<string>https://github.com/[username]/baghdan_sports/blob/main/PRIVACY_POLICY.md</string>
```

### في التطبيق (Flutter):
```dart
const String privacyPolicyUrl = 'https://github.com/[username]/baghdan_sports/blob/main/PRIVACY_POLICY.md';
```

---

## ✅ التحقق من الرفع

بعد الرفع، تحقق من:
1. ✅ الملف موجود في المستودع
2. ✅ الرابط يعمل
3. ✅ المحتوى يظهر بشكل صحيح
4. ✅ التنسيق (Markdown) يعمل

---

## 🎯 نصائح إضافية

### 1. إضافة رابط في README.md
```markdown
## سياسة الخصوصية
اقرأ [سياسة الخصوصية](PRIVACY_POLICY.md) الخاصة بنا.
```

### 2. إنشاء ملف .gitignore
تأكد من عدم رفع ملفات حساسة:
```
# iOS
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate

# Android
*.iml
.gradle
local.properties
.idea/
*.apk
*.ap_

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# Firebase
google-services.json
GoogleService-Info.plist
firebase_options.dart
```

### 3. حماية الملفات الحساسة
لا ترفع:
- ❌ مفاتيح API
- ❌ ملفات Firebase الحساسة
- ❌ كلمات المرور
- ❌ شهادات التوقيع

---

## 📞 المساعدة

إذا واجهت مشاكل:
1. تحقق من أن Git مثبت بشكل صحيح
2. تأكد من أنك مسجل دخول في GitHub
3. تحقق من صلاحيات المستودع
4. راجع رسائل الخطأ في Terminal

---

## 🔄 تحديث سياسة الخصوصية لاحقاً

عند تحديث السياسة:
```bash
# تعديل الملف
nano PRIVACY_POLICY.md

# حفظ التغييرات
git add PRIVACY_POLICY.md
git commit -m "Update privacy policy"
git push origin main
```

---

**ملاحظة:** استبدل `[username]` باسم المستخدم الخاص بك على GitHub في جميع الأمثلة.
